## Available Scripts

In the project directory, you can run:

### `yarn install`

Install all packages

### `yarn dev`

Runs Node server and React in localhost
NB! Check in localhost that ports in package.json proxy and where server is running are the same.

### `yarn build`

Builds the app for production to the `build` folder.
